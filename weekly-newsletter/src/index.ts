const main = async () => {
  const DATE_TAG = '2024-04-04';

  try {
    const articles: string[][] = []
  } catch (error) {
    console.error(`Error while setting up newsletter: ${error}`)
  }
}

main()